{
  "schema_version": 1,
  "base": "order",
  "columns": [
    {"field": "order.code"}
  ],
  "filters": {
    "op": "and",
    "children": [
      {
        "op": "or",
        "children": [
          {"field": "order.code", "operator": "exact", "value": "A"},
          {
            "op": "and",
            "children": [
              {"field": "order.code", "operator": "exact", "value": "B"}
            ]
          }
        ]
      }
    ]
  }
}
