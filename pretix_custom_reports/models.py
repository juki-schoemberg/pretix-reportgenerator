# Owner from wave 1 on: persistence-dev (see ORCHESTRIERUNG.md section 5)
#
# Deliberately empty. bootstrap-dev must not define a single model field --
# the data model is derived from the contracts frozen in wave 0c
# (SPEC.md section 5 is a proposal, not a decision).
#
# persistence-dev also owns the migrations. No migration exists yet and
# bootstrap-dev must not create one.
