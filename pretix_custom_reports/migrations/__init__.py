"""Django migrations for pretix-custom-reports.

Owner from wave 1 on: persistence-dev (ORCHESTRIERUNG.md section 5).

Only this package makes the app a migrated Django app. bootstrap-dev
created the package but deliberately no migration: two migrations
generated in parallel are the classic total loss in this setup.
"""
