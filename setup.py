# Owner from wave 1 on: integrator (see ORCHESTRIERUNG.md section 5)
# Kept as a shim so that `python setup.py sdist` and the pretix-plugin-build
# gettext hook keep working, exactly like in pretix-plugin-cookiecutter.
from setuptools import setup

setup()
